{
  "name": "group5-comp308-community",
  "private": true,
  "workspaces": [
    "shell",
    "mfe-auth",
    "mfe-community",
    "mfe-events"
  ],
  "scripts": {
    "build:remotes": "npm run build -w mfe-auth && npm run build -w mfe-community && npm run build -w mfe-events",
    "start": "npm run build:remotes && npx concurrently -n auth,community,events,shell -c blue,green,yellow,cyan \"npm run preview -w mfe-auth\" \"npm run preview -w mfe-community\" \"npm run preview -w mfe-events\" \"npm run dev -w shell\"",
    "dev": "npm run dev -w shell",
    "build": "npm run build:remotes && npm run build -w shell"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.2.0",
    "@vitejs/plugin-react": "^5.2.0",
    "concurrently": "^9.1.0",
    "vite": "^6.4.2"
  }
}
